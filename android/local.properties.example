# Copy to local.properties (git-ignored) and fill in.
# sdk.dir is set automatically by Android Studio.
sdk.dir=/path/to/Android/sdk

# Supabase — same project as the web/Electron build.
# URL defaults to the project's Supabase if omitted.
SUPABASE_URL=https://cuxblomxefwgdcijmpjk.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
